import { ScrollView, Text, View } from "react-native";

const HomeScreen = () => {
  return (
    <View className="flex flex-col justify-center items-center flex-1">
      <Text className="text-2xl text-cyan-600 text-center px-5">
        Weather App
      </Text>
    </View>
  );
};

export default HomeScreen;
